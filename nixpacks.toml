[phases.build]
cmds = [
    "npm ci",
    "npx prisma generate",
    "npx prisma db push --accept-data-loss",
    "node scripts/deploy-demo.js",
    "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[start]
cmd = "npm start"
