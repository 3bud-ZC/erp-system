[phases.build]
cmds = [
    "npm ci",
    "npx prisma generate",
    "npx prisma db push --accept-data-loss",
    "node scripts/railway-init.js",
    "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[start]
cmd = "npm start"
