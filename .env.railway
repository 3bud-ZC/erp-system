# Railway Environment Variables
# These are set automatically in Railway dashboard:
# - DATABASE_URL (provided by PostgreSQL service)
# - JWT_SECRET (set manually in Variables)
# - NEXTAUTH_SECRET (set manually in Variables)

# DO NOT hardcode secrets here - use Railway dashboard instead!
# Go to: Railway Dashboard → erp-system → Variables

NODE_ENV=production
